
/// A class that contains constants used throughout the application.
class Constants {
  /// The title of the application.
  static const appTitle = "";

  /// The path to the localization files.
  static const localizationPath = 'res/localization';
}

/// A class that contains storage keys used for storing data.
class StorageKeys {
  /// The storage key for authentication data.
  static const authStorageKeyBox = 'auth';

  /// The storage key for authentication data.
  static const authStorageKey = 'auth';

  /// The storage key for the mobile number.
  static const mobileNumberKey = 'mobileNumber';

  /// The storage key for the token.
  static const tokenKey = 'token';

  /// The storage key for the user data.
  static const userKey = 'user';
}